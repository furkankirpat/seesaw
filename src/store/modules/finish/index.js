import * as getters from './getters'
import * as mutations from './mutations'

const state = {
    finishComponentMessage: 'TEBRİKLER KAZANDIN!'
}

export default {
    namespaced:true,
    state,
    getters,
    mutations
}